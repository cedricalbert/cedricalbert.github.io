 copy NUL. nojekyll
 
 quarto render #Render the full site
 
 quarto preview #Force browser preview
 
 
# --- Step 1: Check your current branch name ---
system("git branch") 

# --- Step 2: Initialize and Commit (If you haven't already) ---
system("git add .")
system("git commit -m "Initial commit of ResearchWebsite"")

# --- Step 3: Rename your branch to 'main'---
system("git branch -M main")

# --- Step 4: Push to Origin ---
system("git push -u origin main")





git status
git add .
git commit -m "Update logo"
git push


render error: The Fix: Move the file back
Open your file explorer to D:\OneDrive - Université de Moncton\ResearchWebsite\.

Look inside the docs folder.

If you see join.qmd (and possibly others like index.qmd or research.qmd) inside docs, move them back out to the main ResearchWebsite folder.